spring.application.name=supplyNest-registry-server

server.port=8090

#disable as a client
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Configure peer node URL to avoid connection to default port 8761
eureka.client.service-url.defaultZone=http://localhost:8090/eureka/